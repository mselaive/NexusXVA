import { AppShell } from "@/components/AppShell";

export default function Page() {
  return (
    <AppShell title="Administration" eyebrow="Platform control">
      <section className="panel">
        <div className="empty">User and group administration will be implemented after the BO validation workflow.</div>
      </section>
    </AppShell>
  );
}
